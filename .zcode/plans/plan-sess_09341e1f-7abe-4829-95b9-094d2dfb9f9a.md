Fix the mobile/tablet rendering of the work index page (src/pages/work/index.astro). The sub-1024px fallback is a vertical list of `.zoom-card` panels whose content sits flush against the card edges on phones and whose forced `78svh` height creates dead space. The ≥1024px pinned zoom-carousel contract is left untouched.

Changes (all in src/pages/work/index.astro):

1. `.zoom-card` scoped style (~line 333): add `padding-block: clamp(3.5rem, 10vw, 6.5rem)` so content never touches panel edges, and add a `vh` fallback line before `min-height: 78svh`.
2. Motif wrapper (~line 202): `div.text-brand lg:col-span-5 lg:col-start-8` → add `mx-auto max-w-[18rem] lg:max-w-none` to cap the decorative SVG below lg.
3. Counts numerals (~line 113): `text-[2.25rem]` → `text-[clamp(1.75rem,6vw,2.25rem)]`.
4. `is-zoom` frame (~line 365): add `height: 100vh;` before `height: 100svh;` as a fallback for pre-svh browsers.

Verification: start dev server with `astro dev --background`, check /work at 320px, 390px, 768px and 1280px widths — cards have vertical breathing room, no flush-edge text, no horizontal overflow, desktop carousel unchanged.