# robots.txt for https://zenova.agency
#
# Public marketing pages are crawlable by everything, including AI training and
# AI search crawlers. Only the authenticated application surfaces are excluded.
#
# AI agents: a concise site map written for you is at /llms.txt, and the full
# reference — services, technologies, portfolio, pricing, FAQs — is at
# /llms-full.txt. Both are generated at build time from the site's own data.

User-agent: *
Allow: /
Disallow: /admin/
Disallow: /client/
Disallow: /team/
Disallow: /login/
Disallow: /signin/

# Named AI crawlers, listed explicitly so the permission is unambiguous rather
# than merely implied by the wildcard above.
#
# The repeated Disallow block is deliberate and must stay in sync with the
# wildcard group: a crawler obeys ONLY the most specific group that names it and
# ignores `User-agent: *` entirely. Naming these agents without repeating the
# rules would therefore grant every one of them access to /admin/ — the exact
# opposite of what naming them is meant to achieve.
User-agent: GPTBot
User-agent: OAI-SearchBot
User-agent: ChatGPT-User
User-agent: ClaudeBot
User-agent: Claude-User
User-agent: Claude-SearchBot
User-agent: anthropic-ai
User-agent: PerplexityBot
User-agent: Perplexity-User
User-agent: Google-Extended
User-agent: Applebot
User-agent: Applebot-Extended
User-agent: Amazonbot
User-agent: Bytespider
User-agent: CCBot
User-agent: cohere-ai
User-agent: DuckAssistBot
User-agent: meta-externalagent
User-agent: MistralAI-User
User-agent: YouBot
Allow: /
Disallow: /admin/
Disallow: /client/
Disallow: /team/
Disallow: /login/
Disallow: /signin/

Sitemap: https://zenova.agency/sitemap.xml
