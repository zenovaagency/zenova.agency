node_modules/
*.local

# Next.js
.next/
out/
.vercel/
# next-env.d.ts is deliberately NOT ignored: tsconfig.json references it, and
# committing it is the Next convention (create-next-app does the same). Next
# regenerates it on build, so it never drifts.

# Editor
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store

# Logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Env
.env
.env.*
!.env.example

# Build caches
.tsbuildinfo
*.tsbuildinfo

# Archived pre-refactor sources — kept locally, not for commit
_legacy/
backend/scripts/create_admin_direct.py
SEO-NEXT-STEPS.md
